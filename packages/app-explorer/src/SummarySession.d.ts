import React from 'react';
interface Props {
    withEra?: boolean;
    withSession?: boolean;
}
export default function SummarySession({ withEra, withSession }: Props): React.ReactElement<Props>;
export {};
