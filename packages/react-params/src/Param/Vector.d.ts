import { Props } from '../types';
import React from 'react';
export default function Vector({ className, defaultValue, isDisabled, label, onChange, overrides, style, type, withLabel }: Props): React.ReactElement<Props> | null;
