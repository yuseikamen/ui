import { GeneratorCalculation, GeneratorOptions } from './types';
export default function calculate(test: string[][], _address: string, { atOffset, withCase }: GeneratorOptions): GeneratorCalculation;
