import { KeyringSectionOption } from '@polkadot/ui-keyring/options/types';
import { Option } from './types';
export default function createItem(option: KeyringSectionOption, isUppercase?: boolean): Option;
